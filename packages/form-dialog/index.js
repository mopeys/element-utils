import FormDialog from "./src/main";

// FormDialog.install = function (Vue) {
//   if (FormDialog.install.installed) return;
//   Vue.component(FormDialog.name, FormDialog);
//   FormDialog.install.installed = true;
// };

export default FormDialog;
